# Direct dependencies and compatibility-critical transitive dependencies.
# Update intentionally, then run pip check, warnings-as-errors compilation,
# the full pytest suite, and the golden evaluation before committing.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
pydantic==2.13.4

# MCP 1.28.1 emits an incomplete forward-reference warning with newer
# pydantic-settings releases. 2.12.0 is the verified warning-free boundary.
pydantic-settings==2.12.0
python-dotenv==1.2.3

langchain-core==1.6.0
langchain-ollama==1.1.0
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1

typing_extensions==4.16.0
chromadb==1.5.9
neo4j==6.2.0
mcp==1.28.1

# Test dependencies. Starlette's current TestClient uses httpx2, while the
# application and integrations continue to require httpx.
pytest==9.1.1
httpx==0.28.1
httpx2==2.10.0
