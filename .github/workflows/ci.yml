name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Strict test suite (Python 3.10)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: |
            constraints.txt
            requirements*.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m pip check

      - name: Compile source without warnings
        run: |
          python -W error -m compileall -q src examples scripts tests

      - name: Run tests
        env:
          LLM_PROVIDER: mock
          NEO4J_ENABLED: "false"
          AGENT_API_MAIN_AGENT_MCP_ENABLED: "false"
        run: |
          python -m pytest -q

      - name: Run ERP business golden acceptance
        env:
          LLM_PROVIDER: mock
          NEO4J_ENABLED: "false"
          AGENT_API_MAIN_AGENT_MCP_ENABLED: "false"
        run: |
          python scripts/run_erp_diagnosis_eval.py

      - name: Run Synthetic ERP HTTP end-to-end validation
        env:
          LLM_PROVIDER: mock
          NEO4J_ENABLED: "false"
          AGENT_API_MAIN_AGENT_MCP_ENABLED: "false"
        run: |
          python scripts/run_erp_http_e2e.py
